# Frontend ENV (Vite + React)
#
# Default (kosong) = panggil API dengan same-origin (/api) atau lewat proxy dev.
#

# Jika backend kamu beda domain/port, isi URL lengkap backend:
# VITE_API_BASE="https://inventaris-backend-xxxx.up.railway.app"
VITE_API_BASE=""

